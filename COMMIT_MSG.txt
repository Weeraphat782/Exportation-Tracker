feat(seo): Phase 1 SEO and GEO implementation

- Root metadata: metadataBase, OG, Twitter, viewport, robots snippets, verification
- Per-page metadata on all 12 marketing pages with canonical URLs
- JSON-LD structured data: Organization, WebSite, WebPage, BlogPosting, Article, ItemList, BreadcrumbList
- sitemap.ts with static + dynamic routes from Supabase
- robots.ts with AI crawler allowances (GPTBot, Claude-Web, PerplexityBot, etc.)
- llms.txt route with entity info, key pages, citation guidance
- RSS feed.xml route pulling news_articles from Supabase
- Custom 404 page with noindex
- Security headers, cache headers, poweredByHeader:false, compress:true
- TL;DR aside blocks and At a Glance tables on hub pages
- Entity consistency: standardize OMG Experience across all marketing touchpoints
- Semantic HTML upgrades: article, section, aside, time, nav elements
- Hub-and-spoke linking: services hub to subpages, ContinueExploring on articles
- BreadcrumbNav component with matching BreadcrumbList JSON-LD
- Freshness signals: datePublished/dateModified in JSON-LD + OG, lastModified in sitemap
- Public API summary endpoint with CORS for AI agents
- manifest.json for PWA support
